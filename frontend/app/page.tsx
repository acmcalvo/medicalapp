import { InteractionWorkspace } from '@/components/interaction-workspace';

export default function HomePage() {
  return (
    <main className="page-shell">
      <InteractionWorkspace />
    </main>
  );
}
